module.exports = {
    name: 'role',
    args: true,
    usage: '<usuario> <rol>',
    description: 'Information about users roles.',
    execute(message, args) {
        /*
        if (args[0] === 'foo') {
            return message.channel.send('bar');
        }

        message.channel.send(`Arguments: ${args}\nArguments length: ${args.length}`);
        */
    },
};